include "include/mlg user interface.pixih"
include "include/mlg easy programming.pixih"

COLOR_THEME=THEME_LIGHT
win=new_window("Important!", -200, -100, 400, 200, WINDOW_ALERT)
win.color=#80FFFF
text=new_text("U SUS!!!", 4, 4, win.width-2, 6)
text.align=TOP|LEFT
text.text_align=TOP|LEFT
bind_text(text, win)
while 1
{
	clear(#FFFFFF)
	draw_window(win)
	frame()
	while get_event()
	{
		get_button_event(butt)
		if EVT[EVT_TYPE]==EVT_QUIT
		{
			halt
		}
	}
}