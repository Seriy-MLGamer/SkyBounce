start_timer()
while 1
{
	clear(#808080)
	fbox(-200, -100, 400, 200, GREEN)
	effector(EFF_HBLUR, 64-get_timer()/1000*16, 0, -300, -200, 600, 400)
	effector(EFF_VBLUR, 64-get_timer()/1000*16, 0, -300, -200, 600, 400)
	frame()
	while get_event()
	{
		if EVT[EVT_TYPE]==EVT_QUIT
		{
			halt
		}
	}
}